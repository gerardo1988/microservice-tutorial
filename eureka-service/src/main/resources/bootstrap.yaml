spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
eureka:
  server:
    renewal-percent-threshold: 0.85
    enable-self-preservation: false
